/**
 * 同步示例
 * @param params 后期优化为接口
 */
export const userRegister1 = async (params: any) => {
  // http请求
  // let res = await =
  return {
    type: 'USERINFO',
    payload: {},
  };
};

/**
 * 同步示例
 * @param params 后期优化为接口
 */
export const userRegister2 = async (params: any) => {
  // http请求
  // let res = await =
  return {
    type: 'USERINFO',
    payload: {},
  };
};

// export const userRegister3 = (params: any) => {
//   // http请求
//   // let res = await =
//   return (dispatch: any) => {
//     // 更新获取数据状态
//     dispatch(Promise.resolve('a'));
//     return httpUtil
//       .get('url')
//       .then((res) => {
//         // 更新获取数据状态、设置数据
//         dispatch(Promise.resolve('a'));
//       })
//       .catch((e) => {
//         // 更新获取数据状态、错误信息
//         dispatch(Promise.reject('a'));
//       });
//   };
// };

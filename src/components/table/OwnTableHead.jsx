import TableHead from '@material-ui/core/TableHead';
import { withStyles } from '@material-ui/core/styles';

const OwnTableHead = withStyles((theme) => ({
  root: {},
}))(TableHead);

export default OwnTableHead;

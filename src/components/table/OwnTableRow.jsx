import TableRow from '@material-ui/core/TableRow';
import { withStyles } from '@material-ui/core/styles';

const OwnTableRow = withStyles((theme) => ({
  root: {},
}))(TableRow);

export default OwnTableRow;
